[package]
name = "soroban-perpetuals"
version = "0.1.0"
edition = "2021"
description = "Perpetual futures vAMM funding-rate engine"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "21.0.6"

[dev-dependencies]
soroban-sdk = { version = "21.0.6", features = ["testutils"] }
