[package]
name = "royalty"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Tiered co-creator royalty waterfall engine for Soroban"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "21.0.6"

[dev-dependencies]
soroban-sdk = { version = "21.0.6", features = ["testutils"] }

